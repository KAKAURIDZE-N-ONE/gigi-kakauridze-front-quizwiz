export type FormValuesLogin = {
  email: string;
  password: string;
};

export type FormValues = {
  name: string;
  email: string;
  password: string;
  passwordRepeat: string;
};

export type ForgotPassword = {
  email: string;
};
